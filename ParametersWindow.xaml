<Window x:Class="RevitParametersAddin.ParametersWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitParametersAddin"
             mc:Ignorable="d" 
             Height="auto" MinHeight="300" Width="800" ResizeMode="CanResize" VerticalAlignment="Center" Title="Create New Parameters">
    <Border Padding="15">
        <StackPanel>
            <Label Content="Create New Parameter" FontSize="30px" FontWeight="Bold" Margin="5 0" HorizontalAlignment="Center"></Label>
            <Separator></Separator>

            <StackPanel Orientation="Horizontal">
                <Grid Width="800" Margin="10 10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="80"></ColumnDefinition>
                        <ColumnDefinition Width="240"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock  Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" TextAlignment="Left">Name</TextBlock>
                    <TextBox Name="ParameterName" Grid.Row="0" Grid.Column="1" Margin="10 10" Width="200" HorizontalAlignment="Left"></TextBox>
                    <Button Content="Create New Parameter" Grid.Row="0" Grid.Column="3" Width="150" Background="Black" Foreground="White" Cursor="Hand" Click="AddNewParameter"></Button>
                    <!--<Button Content="Use Shared Parameter" Width="100" Grid.Column="2" Background="Black" Foreground="White" Cursor="Hand" Click="AddParameter"></Button>
                    <Button Content="Remove" Width="100" Grid.Column="4" Background="Black" Foreground="White" Cursor="Hand" Click="RemoveParameter"></Button>-->

                </Grid>
                <Separator></Separator>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
